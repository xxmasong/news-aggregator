import * as React from 'react';
import EmailLogin from '@/components/organisms/etc/EmailLogin';

const EmailLoginPage: React.FC<{}> = props => {
  return <EmailLogin />;
};

export default EmailLoginPage;
